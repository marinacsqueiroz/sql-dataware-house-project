name: sql_dataware_house_project
version: 1.0.0
profile: sql_dataware_house_project
config-version: 2

models:
  sql_dataware_house_project:

    staging:
      +schema: bronze
      +materialized: table

    marts:
      +schema: silver
      +materialized: view
